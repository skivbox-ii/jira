# Agent Notes

- For dashboard user multi-selects, reuse the Timesheet dropdown pattern: the button shows the selected count, selected users are pinned as removable chips at the top of the dropdown, selected rows are highlighted and sorted before the rest, and search keeps selected users visible so they are easy to remove.
