# Перенос текста и раздельное исключение готовых: 25.09.2026

## Изменения

- В столбце «Замечание из Excel» сняты все ограничения количества строк:
  текст переносится целиком, включая свёрнутые группы и незаведённые замечания.
- «Исключить готовые» действует только на дочерние задачи. Исходная история
  остаётся видимой, даже если готова сама или все её дочерние задачи скрыты.
- «Исключить готовые истории» скрывает всю группу готовой исходной истории,
  включая незавершённые дочерние задачи. Для незаведённых замечаний старый
  статус из Excel не является основанием скрывать строку.
- Галочки независимы, применяются по ОК. Отмена не меняет фильтр; «Снять
  фильтр» и общий сброс очищают обе. Индикация активна при любой галочке.
- Точные фильтры значений и показ родителя для контекста сохранены.
  Список статусов не сокращается из-за этих двух галочек; остальные списки
  учитывают скрытые группы. Запись в Jira не добавлялась и не менялась.

## Проверки

- Регрессионные тесты до исправления воспроизвели обрезание текста и исчезновение
  незаведённого замечания со старым готовым статусом Excel.
- Полный прогон: 1186 тестов, 1186 прошли, 0 ошибок.
  Лог: `/tmp/ujg-reg02-full.tap`.
- Независимое ревью: замечаний нет; ревьюер отдельно проверил 106 тестов.
- В реальном локальном браузере 1440x1000 проверены раскрытые, свёрнутые и
  новые строки: высота текста равна полной высоте содержимого, clamp отсутствует.
- Проверены все четыре комбинации на синтетической фикстуре: без галочек 71
  замечание; исключение готовых детей сохраняет 71, включая готовые истории;
  исключение готовых историй оставляет 54, но сохраняет 47 видимых готовых
  детей открытых историй; обе галочки оставляют 54 без готовых детей.
  Незаведённые строки остаются во всех комбинациях, сброс возвращает 71.
- Экран 390x844: текст переносится целиком, обе галочки и действия фильтра
  помещаются, горизонтального переполнения страницы нет (390/390).
- Собраны только importer JS/runtime, `git diff --check` без ошибок.

## Файлы

`ujg-excel-story-importer-modules/registry.js`,
`ujg-excel-story-importer-modules/grid.js`, `ujg-excel-story-importer.css`,
сгенерированные JS/runtime; тесты registry, grid и UI-source.
Правила закреплены в `docs/import-table-ui-contract.md`.

## Публикация и прод

Изменения опубликованы в `origin/main`: `25751d7`. Runtime и CSS скачаны
с commit-pinned URL jsDelivr и побайтово совпадают с локальной сборкой.
В Citrix в 10:36 МСК открыт Outlook; управление рабочим экраном пользователя
не перехватывалось.
Снимок: `/Users/skivbox/.codex/citrix-prod-runs/import-reg02-20260925/screenshots/inspect-20260925-103620-14938.png`.
Следующий шаг: проверить перенос текста и две галочки в реестре Jira через
Citrix, не изменяя тикеты. Локальная проверка не подменяет прод-приёмку.
