# Колонка «Модуль» в динамике

## Объём ACT-06

Отдельная видимая колонка после «Тикет · Роль». Источник: текущие компоненты
исходной истории Jira. Компоненты ребёнка не подменяют модуль замечания.
Все названия показаны полностью, отдельными строками. Пустой набор обозначен
«Без компонента», отсутствующие данные обозначены «Компоненты неизвестны».

Фильтр предлагает отдельные компоненты с поиском и мультивыбором; совпадения
объединяются по ИЛИ. Это фильтр журнала, он не меняет общий срез и LLM.
Сортировка по русскому алфавиту перемещает целые группы замечаний.
Ширина, порядок, видимость и фильтр сохраняются в пользовательской раскладке.
Старые раскладки получают видимый модуль без сброса остальных настроек;
последующее явное скрытие сохраняется. HTML включает те же значения и порядок.

## Проверки до публикации

- Полный прогон: **1346/1346**, exit 0, `/tmp/ujg-module-column-final.tap`.
- TDD: отсутствующая колонка, фильтр и миграция воспроизведены до реализации.
  Отдельная регрессия воспроизвела неверный порядок «Единый, Журнал, Ёлка»;
  после исправления порядок «Единый, Ёлка, Журнал», проверены обратная
  сортировка и порядок HTML.
- Независимое ревью: дефект русской сортировки исправлен; повторный прогон
  ревьюера 11/11, оставшихся замечаний нет.
- Лично на локальном стенде: видимая колонка, АСУТП у исходной истории и
  её детей, хотя компонент ребёнка другой; фильтр АСУТП оставляет 1 замечание
  и 6 событий; сброс возвращает полный журнал. Сортировка и перезагрузка
  сохраняют порядок. На 390x844 попап со списком и кнопкой применения
  помещается в экран; широкая таблица прокручивается внутри своей области.
- Скриншоты: `/tmp/import-components-proof/local-module-column.png` и
  `/tmp/import-components-proof/local-module-column-narrow.png`.
- HTML проверен автотестами, включая экранирование, несколько компонентов,
  пустые/неизвестные данные и фильтр. Скачивание через браузерный инструмент
  не подтверждено: ожидание события download завершилось таймаутом.

Команда полного прогона:

```sh
env NODE_PATH=/tmp/ujg-import-registry-test/node_modules node --test --test-concurrency=1 --test-reporter=tap tests/*.test.js tests/browser/excel-story-importer-*.test.cjs tests/browser/import-preview-*.test.cjs
```

## Производственная приёмка

Опубликовано `9907ef8`; commit-pinned CDN runtime побайтово совпал с локальной
сборкой. Личная read-only приёмка через `testing-citrix-prod` завершена
26.09 в 19:08 МСК. Сессия:
`/Users/skivbox/.codex/citrix-prod-runs/import-module-column-20260926/`.

- После обновления страницы и перехода в «Динамику», без Excel, автоматически
  загружены 509/509 историй задач. Срез на 19:02: 24 замечания с изменениями,
  82 события, 11 стали готовы. Новая колонка появилась в существующей
  раскладке после «Тикет · Роль», прежняя увеличенная ширина темы сохранена.
- Попап содержит сортировку, поиск, общий чекбокс, «Без компонента» и
  применение. В текущем дневном журнале все исходные истории без компонента.
  Снятие чекбокса не закрывает меню; применение даёт 0 замечаний/0 событий
  только в журнале. Общие итоги остаются прежними. Сброс возвращает 24/82.
- Последовательно прокручены все 24 группы с перекрытием кадров от 2795
  до 2775. Колонка остаётся на своём месте у исходной истории и дочерних
  задач, без наложений на соседние ячейки. Завершение по-прежнему зелёное,
  действия минутных групп читаемы. В конце возвращено начало страницы.
- Широкая пользовательская раскладка требует внутренней горизонтальной
  прокрутки для крайнего столбца; ширины и порядок других колонок не сброшены.
- Реальные компоненты, назначения и другие данные Jira не изменялись.
  Успешное отображение непустых и нескольких компонентов проверено локально
  и автотестами; текущий продовый дневной срез такого примера не содержит.
  REG-03, сопоставление фактического модуля Excel, этим исправлением не закрыта.

Скриншоты в каталоге `screenshots/` этой сессии:

- `inspect-20260926-190309-22505.png`: полный срез и видимая колонка.
- `inspect-20260926-190416-09360.png`: фильтр.
- `inspect-20260926-190438-08046.png`: снятый выбор, попап остаётся открыт.
- `inspect-20260926-190501-06732.png`: 0/0 строк, общие итоги неизменны.
- `inspect-20260926-190529-21773.png`: сброс, снова 24/82.
- `inspect-20260926-190545-04277.png`, `inspect-20260926-190633-31267.png`,
  `inspect-20260926-190654-03603.png`, `inspect-20260926-190715-10524.png`,
  `inspect-20260926-190737-16537.png`, `inspect-20260926-190756-21641.png`,
  `inspect-20260926-190813-23111.png`: полный последовательный просмотр.
- `inspect-20260926-190831-12942.png`: возврат к началу после проверки.
