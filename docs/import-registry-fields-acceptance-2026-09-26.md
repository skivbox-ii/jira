# Реестр, приоритет детей и группировка динамики

Дата: 26.09.2026. Статус: код опубликован и проверен; настройка исходного модуля и некорректных пользовательских сопоставлений остаётся открытой.

## Объём

- Срок исполнения Excel в реестре: строгий существующий разбор дат,
  сортировка календарных дат, фильтр, сохранение видимости и ширины.
  Пустые и неоднозначные значения не заменяются выдуманной датой.
- Модуль Excel сопоставляется с компонентом; фактические компоненты Jira
  загружаются отдельно от ожидаемого значения. Справочник проверяется
  для выбранного проекта, поздний ответ другого проекта игнорируется.
  Дочерняя задача показывает собственные компоненты, не компоненты истории.
- Перед дозаведением читается текущий приоритет родительской истории.
  После создания новой истории её приоритет читается из Jira перед детьми.
  Ошибка чтения не допускает создания детей с произвольным приоритетом.
- Журнал динамики объединяет события одной задачи, одного идентифицированного
  автора и одной минуты. Подробности раскрываются; завершение зелёное.
  Исходные события, счётчики, LLM-контекст и подробный экспорт не сокращаются.

## Личная проверка локального интерфейса

Стенд `http://127.0.0.1:4317/`, только синтетические данные.
Проверены настройки столбцов со сроком и компонентами, загрузка справочника
компонентов, сообщение о неизвестных сопоставлениях, раскрытие двух событий
09:15 у EVOSCADA-18080, зелёное завершение EVOSCADA-18057 в 11:10.
Передача исполнителя и переход статуса в раскрытой строке сохранены.

## Citrix

Только навык `testing-citrix-prod`, сессия `import-registry-fields-20260926`.
Снимок до изменений `inspect-20260926-160100-04716.png`: отдельные строки
передачи и завершения. `inspect-20260926-160648-26854.png`: отдельные события
одной задачи в 13:24 и в 15:03. Реальные задачи не изменялись.

## Автотесты и ревью

Полный прогон после финальной сборки: 1297/1297, exit 0,
`/tmp/ujg-registry-fields-full.tap`. Запись Jira тестировалась только на заглушках.
Независимое ревью выявило потерю компонентов дочерних задач при нормализации
и перекрытие существенного расхождения компонентов предупреждением о регистре
имени. Оба случая исправлены и покрыты регрессиями.
Повторное ревью: отмена записи после смены контекста во время чтения родителя,
нейтральное отображение Done → Done, отдельная диагностика точного имени
компонента вместо ложного сообщения об отсутствии. Регрессии пройдены.
Лично проверен фильтр срока: выбор 26.09.2026 оставляет одну историю
и её дочерние задачи; сброс возвращает реестр.

## Публикация и личная приёмка Citrix

Код: `a378462a2e6096c0af621208c1e84dc2921a47f9` (короткий идентификатор `a378462`).
Runtime и CSS проверены побайтово через commit-pinned CDN после публикации.
26.09, 16:23–16:33: обновлена страница, выбран первый журнал от 25.09,
выполнена только читающая синхронизация из Jira. Импортированы 72 строки.
Снимки находятся в `~/.codex/citrix-prod-runs/import-registry-fields-20260926/screenshots/`:

- `inspect-20260926-162538-19638.png`: срок Excel и компонент Jira доступны
  и включены в настройках столбцов.
- `inspect-20260926-162851-05160.png`: сроки 24, 26 и 29 сентября на реальных
  историях; отсутствующие компоненты честно показаны прочерком.
- `inspect-20260926-163149-22890.png`: справочник компонентов выбранного
  проекта загружен. Существующие сопоставления на `1`, `АСУТП`, `PNGI`,
  `Multimedia2`, `Эвоскада замечания` не найдены в нём. `АСУТП` используется
  двумя строками настройки. Пользовательские настройки не перезаписывались.
- `inspect-20260926-163202-23451.png`: источник модуля настроен на колонку
  `Модуль`, срока на `Планируемая дата устранения`. В показанных строках
  модуль пуст; фактическое имя и заполненность исходной колонки ещё требуют
  проверки по журналу. Поле `Тип` не подставлялось вместо модуля.
- `inspect-20260926-163236-23548.png`: вход в динамику автоматически загрузил
  историю 507/507 задач. События EVOSCADA-21817 в 15:03 сгруппированы.
- `inspect-20260926-163307-29271.png`: раскрытая группа сохраняет создание
  задачи и добавление связи, время и исполнителя.
- `inspect-20260926-163334-12337.png`: EVOSCADA-21745, 13:21, зелёная строка
  «Задача выполнена · 2 изменения»; раскрытие сохраняет результат и переход
  статуса. Соседняя история EVOSCADA-21637 имеет отдельную группу 13:24.

Приоритет новых детей проверен автотестами со свежим чтением родителя и
защитой от смены контекста. Реальная запись в Jira намеренно не выполнялась:
это не подтверждение создания производственного тикета.

Не закрыт остаток REG-03: установить фактический источник модуля в исходном
журнале и согласованные соответствия отсутствующих компонентов. Нельзя
считать прочерки доказательством корректного заполнения или выбирать похожие
компоненты без основания. ACT-03 лично проверена на проде.
