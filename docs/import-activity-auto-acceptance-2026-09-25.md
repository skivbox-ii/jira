# ACT-01: Приёмка автоматической загрузки истории

Дата: 25.09.2026, 12:01–12:03 МСК. Версия: `7005180`.
Проект EVOSCADA, эпик EVOSCADA-21054, dashboard 12817.

## Изменение

Вход в «Динамику» обновляет текущий срез Jira и дочитывает неполные истории.
Дата отчёта сохраняется; смена даты использует полную историю или дочитывает
недостающие данные. До трёх параллельных чтений, без автоматического цикла
повторных ошибок. Запоздавшие ответы старого среза игнорируются.

Расхождение changelog/updated в пределах той же секунды больше не означает
пропуск истории. Исходные времена сохранены в неблокирующей диагностике.
Другой секундный интервал, неполная пагинация, повреждённые записи и
противоречия цепочки статусов по-прежнему блокируют полноту.

## Проверки

- Регрессии сначала воспроизвели отсутствие дозагрузки и ошибочную оценку
  миллисекунд. Независимое ревью дополнительно обнаружило смену дня во время
  загрузки; исправление проверено отдельным регрессионным тестом и ревьюером.
- Полный набор: 1193/1193, `/tmp/ujg-act01-full.tap`; повторный независимый
  прогон enrichment: 80/80. `git diff --check` без замечаний.
- Локальный стенд, 1440x900 и 390x844: неполная история автоматически
  дочитана один раз; 227/227; прогресс над итогами; дата 24.09 сохранена
  при повторном входе. Диагностика переносится, ширина документа 390px
  при ширине окна 390px. Сквозной тест использует только заглушки.
- Публикация в main. Commit-pinned CDN совпадает с локальными файлами:
  runtime SHA-256 `6c96eaeb8df1780dc4361af963ceeac2be25c1a0739f7111012f63aca9223a62`;
  CSS `30a7b713af88aa7bdea9fbf3594d36f0a3ac398860cd3dc9d07631b29ebb7f80`.

## Личная проверка в Citrix

Использован только `testing-citrix-prod`, сессия
`import-activity-auto-20260925`. Обновлена страница, затем нажата «Динамика»
из пустого Excel-режима. Журнал не загружался; кнопка ручного обновления
истории не нажималась.

1. Вход: 487/487 проверенных историй, день 25.09, 157 событий и 7 готовых
   замечаний на 12:01. Снимок `inspect-20260925-120125-15500.png`.
2. Предыдущий день: 24.09, 487/487, 52 замечания с изменениями,
   11 стали готовы, 16 задач с возвратами, 346 событий.
   Снимок `inspect-20260925-120200-21455.png`.
3. Раскрыта диагностика: 453 записи с исходными временами, внутренний
   скроллинг. Например EVOSCADA-21776: `.628Z` против `.000Z` в поисковом
   снимке. Это число диагностических записей, не число неполных задач.
   Снимок `inspect-20260925-120234-26501.png`.
4. Переход в реестр и обратно: сохранился день 24.09 и 487/487,
   те же 346 событий и 11 готовых замечаний.
   Снимки `inspect-20260925-120257-27062.png` и
   `inspect-20260925-120327-32166.png`.

Каталог доказательств:
`/Users/skivbox/.codex/citrix-prod-runs/import-activity-auto-20260925/screenshots/`.
До исправления на том же эпике было 109/487 и 378 блокирующих предупреждений.

## Границы Приёмки

Тикеты, статусы, назначения, связи и сроки не менялись. Живого вызова LLM
не было. Проверка подтверждает исправление ACT-01 на указанном срезе,
а не отсутствие любых будущих ошибок Jira. Чтение истории не восстанавливает
исторический состав связей и не угадывает нераспознанные сроки.

Ошибки сроков в режиме без Excel остаются видны отдельно; это не ошибки
полноты истории. DUE-02 с первоначальными 39 сроками и новые функции Due date
не считаются выполненными этой приёмкой.
