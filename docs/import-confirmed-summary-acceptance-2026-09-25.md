# ACT-02: Подтверждённые итоги без знаков сравнения

Дата: 25.09.2026. Статус: локальная приёмка и ревью пройдены;
публикация и личная проверка новой сборки в Citrix пока не завершены.

## Изменения

- Счётчики, окна метрик, HTML-выгрузка и фиксированный заголовок LLM
  используют подтверждённые итоги. Знаки сравнения убраны. Стрелка,
  похожая на знак «меньше», заменена на Lucide Undo2 с подсказкой.
- Неполная история одной задачи исключает только затронутые замечания
  из итогов истории. Видны покрытие, ключи исключённых замечаний и причины.
  Все наблюдаемые события и просрочка имеют отдельное покрытие.
  Строгие глобальные показатели остаются неизвестными при неполноте.
- Без единого подтверждённого замечания история не показывает ложные нули.
  Готовность отдельной задачи не считается готовностью всего замечания.
- Текущий баланс подписан «на момент загрузки», полный прошедший день
  «на конец дня». Миллисекундные расхождения скрыты в технических подробностях;
  ошибки распознавания сроков названы «Не распознаны сроки».
- Реальное расхождение EVOSCADA-21769: changelog 10:53:04.000Z,
  updated 10:53:03.998Z, всего 2 мс. Допуск теперь проверяет разность меньше
  секунды, а не одинаковый номер секунды. Времена не изменяются;
  разница от 1000 мс остаётся блокирующей. Это уточняет ACT-01.
- Старый снимок не сдвигает общий текущий срез на предыдущий день,
  если есть свежие снимки. Старые данные по-прежнему исключаются.

## Проверки

TDD: новые проверки сначала падали. Проверены смешанная полнота, неизвестные
статусы, неполный состав, конфликтующие/будущие/старые снимки, отсутствие
подтверждённых групп, общие дочерние задачи, границы 2/999/1000/1001 мс,
выгрузка и LLM с заглушками.

Независимое ревью (Russell, gpt-6-sol medium) обнаружило и повторно проверило:
старый снимок обнулял текущий срез; общий возврат дублировался в окне метрики;
инструкция LLM использовала строгую просрочку вместо подтверждённой.
Все три замечания устранены, финальных замечаний нет.

Полный регрессионный прогон после финальной сборки: **1258/1258**, exit 0,
`/tmp/ujg-act02-full.tap`. Фокусные browser-тесты: 139/139; AI: 24/24.
`git diff --check` без замечаний. Пересобраны только importer и его runtime.

Личная локальная проверка в Playwright на `http://127.0.0.1:4317/`,
1440x900 и 390x844, с разрешённым только localhost и подставными данными:

- Полный срез: 227/227 задач, 4 изменённых, 1 новое, 1 готовое, 3 возврата,
  17 событий, 2 просроченных замечания.
- Неполный срез: 226/227 задач, 68/69 замечаний; 3 изменённых, 1 новое,
  1 готовое, 1 возврат, 17 событий, 2 просроченных. EVOSCADA-16104
  перечислена в исключениях с причиной.
- Окно готовых совпадает со счётчиком, прошлый день имеет правильную подпись.
  Диагностика свёрнута, нет горизонтального переполнения или ошибок JavaScript.
  Записей в Jira и вызовов LLM: 0.

Снимки: `/tmp/ujg-confirmed-summary-acceptance/`, сценарий
`/tmp/ujg-confirmed-summary-visual.cjs`. Лично просмотрены desktop,
mobile и окно метрики.

## Citrix

Используется только навык `testing-citrix-prod`, сессия
`import-confirmed-summary-20260925`. Начальное состояние 15:10 и 15:17 МСК:
EVOSCADA, EVOSCADA-21054, dashboard 12817; 491/492, ложная ошибка 2 мс,
знаки сравнения и неизвестная готовность. Снимки `inspect-20260925-151058-15162.png`
и `inspect-20260925-151725-25004.png` в каталоге сессии.
Приёмка новой опубликованной версии будет записана отдельно после проверки.

## Файлы И Границы

Модули: `activity.js`, `activity-ai.js`, `activity-ui.js`,
`activity-management-ui.js`, `icons.js`; importer CSS, bundle и runtime;
core/AI и два browser-теста; план, бэклог и этот отчёт.

Реальная запись в Jira и живой вызов LLM запрещены в этой проверке.
Нераспознанные сроки не исправляются подменой данных. DUE-02, DUE-03,
DUE-04 и колонка срока в Excel не закрываются этой приёмкой.
Регулярная автоматизация остаётся остановленной.
